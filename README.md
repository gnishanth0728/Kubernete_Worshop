# Kubernete_Worshop


A pod in kubernetes can have two container share
# Shared Network
+ Shared Storage (optional)
+ Shared Lifecycle
+ Shared Scheduling
+ One Logical Application Unit

That's why Kubernetes schedules Pods, not individual containers. A Pod represents a set of containers that need to behave like they are running on the same machine.
